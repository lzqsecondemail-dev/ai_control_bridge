# Examples

This directory describes public-safe examples for task cards and local
execution reports.

Example payloads must use fake identifiers and placeholder paths. They must not
include real private task IDs, credentials, local reports, personal directories,
or machine-specific project paths.

See:

- `examples/sample-task-cards/`
- `examples/sample-reports/`
